import React from 'react';

import { beerObj } from '../types/index.js';

import './beerDetails.scss';

const BeerDetails = ({ beerDetailed }) => <div />;
BeerDetails.propTypes = {
  beerDetailed: beerObj,
};

export default BeerDetails;
